# Custom Branding & Icons

Place your custom branding assets here:

- `logo.png` (or `logo.svg`): Custom logo shown in the top-left navigation bar (auto-detected, with SVG fallback).
- `favicon.png` (or `favicon.ico`): Web browser tab icon.

Any files added in this folder are automatically served statically under `/static/img/<filename>`.
